import { HashLoader } from "react-spinners";

function Spinner() {
  return (
    <div className="h-screen w-full flex items-center justify-center">
      <HashLoader color="#29ADB2" />
    </div>
  );
}

export default Spinner;
